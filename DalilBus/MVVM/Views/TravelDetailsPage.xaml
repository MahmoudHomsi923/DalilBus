<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DalilBus.MVVM.Views.TravelDetailsPage"
             Title="Travel Details تفاصيل الرحلة">

    <ContentPage.Resources>
        <x:String x:Key="calender">&#xf073;</x:String>
        <x:String x:Key="hourglass">&#xf251;</x:String>
    </ContentPage.Resources>

    <ScrollView>
        <Grid VerticalOptions="Fill" HorizontalOptions="Fill">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid 
            Grid.Row="0"
            ColumnDefinitions="*,Auto,*"
            HeightRequest="40">
            <HorizontalStackLayout 
                Grid.Column="0" 
                Spacing="5" 
                HorizontalOptions="Center" 
                VerticalOptions="Center">
                <Label 
                    Text="{StaticResource calender}"
                    FontFamily="FAS"
                    FontSize="16"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
                <Label
                    x:Name="lblSelectedTravelDepatureDate"
                    Text="{Binding SelectedTravel.DepatureDateDisplay}"
                    FontSize="16"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
            </HorizontalStackLayout>
            
            <HorizontalStackLayout 
                Grid.Column="2" 
                Spacing="5" 
                HorizontalOptions="Center" 
                VerticalOptions="Center">
                <Label 
                    Text="{StaticResource hourglass}"
                    FontFamily="FAS"
                    FontSize="16"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
                    <Label
                    x:Name="lblSelectedTravelDuration"
                    Text="{Binding SelectedTravel.DurationDisplay}"
                    FontSize="16"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
            </HorizontalStackLayout>
        </Grid>

        <Grid 
            Grid.Row="1"
            ColumnDefinitions="*,Auto,*"
            HeightRequest="40">
                <HorizontalStackLayout 
                Grid.Column="0" 
                Spacing="5" 
                HorizontalOptions="Center" 
                VerticalOptions="Center">
                    <Label 
                    Text="{StaticResource calender}"
                    FontFamily="FAS"
                    FontSize="25"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
                    <Label
                    x:Name="lblSelectedTravelStartPlace"
                    Text="{Binding SelectedTravel.StartPlaceDisplay}"
                    FontSize="30"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
                </HorizontalStackLayout>

            </Grid>

        </Grid>
        
    </ScrollView>
</ContentPage>